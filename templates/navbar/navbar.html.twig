<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_film') }}">Films</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('app_film_create') }}">Create film</a>
                        </li>
                        <a href="{{ path('app_profile') }}" class="nav-link">{{ app.user.username }}</a>
                        <a class="nav-link ms-3" href="{{ path('app_logout') }}">Logout</a>
                    {% elseif is_granted('ROLE_USER')  %}
                        <a href="{{ path('app_profile') }}" class="nav-link">{{ app.user.username }}</a>
                        <a class="nav-link ms-3" href="{{ path('app_logout') }}">Logout</a>
                    {% endif %}
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                        </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>